# Copy and paste this ENTIRE command into your terminal:

cd /Volumes/LaCie/Coding-Projects/queryable-slack && (echo "🔍 Conductor Ingestion Monitor"; echo "================================"; echo ""; PID=$(ps aux | grep -E "python.*conductor.ingest" | grep -v grep | awk '{print $2}' | head -1); if [ -n "$PID" ]; then echo "🔄 Process Status:"; ps -p $PID -o pid,pcpu,pmem,etime 2>/dev/null | tail -1 | awk '{print "   PID:", $1, "| CPU:", $2"% | Memory:", $3"% | Runtime:", $4}'; else echo "⚠️  No ingestion process found"; fi; echo ""; if [ -f ~/.conductor_ingestion_checkpoint.json ]; then echo "📊 Checkpoint Status:"; cat ~/.conductor_ingestion_checkpoint.json | python3 -c "import json,sys; d=json.load(sys.stdin); c=len(d.get('completed',{})); f=len(d.get('failed',{})); ff=d.get('failed_files',{}); print(f'   ✅ Completed: {c}'); print(f'   ❌ Failed convos: {f}'); print(f'   ⚠️  Failed files: {len(ff)}'); et={}; [et.update({info.get('error_type','unknown'):et.get(info.get('error_type','unknown'),0)+1}) for info in ff.values()]; [print(f'      - {k}: {v}') for k,v in sorted(et.items(),key=lambda x:-x[1])]" 2>/dev/null || echo "   ⚠️  Error reading checkpoint"; else echo "📝 No checkpoint file"; fi; echo ""; echo "📋 Live Log:"; tail -f ingestion_resume.log)

