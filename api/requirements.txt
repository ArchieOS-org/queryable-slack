# Vercel serverless function dependencies
# Minimal dependencies for API function only (excludes large ML packages)

# ASGI adapter for Vercel
mangum>=0.17.0

# Core API dependencies
pydantic>=2.0,<3.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database clients (required for migration)
vecs>=0.4.0  # Supabase pgvector client
psycopg[binary]>=3.1.0  # PostgreSQL adapter for migration

# AI/LLM (minimal - only for API responses)
anthropic>=0.34.0

# Utilities
python-dotenv>=1.0.0
